<?xml version="1.0" encoding="utf-8"?>
<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <Transition
        app:constraintSetEnd="@id/end_set"
        app:constraintSetStart="@id/start_set"
        app:duration="3000">

        <OnSwipe
            app:dragDirection="dragLeft"
            app:touchAnchorId="@+id/button"
            app:touchAnchorSide="top" />
    </Transition>

    <ConstraintSet android:id="@+id/start_set">
        <Constraint
            android:id="@+id/button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintRight_toRightOf="parent">
            <CustomAttribute
                app:attributeName="backgroundColor"
                app:customColorValue="#D81B60" />
        </Constraint>
    </ConstraintSet>

    <ConstraintSet android:id="@+id/end_set">
        <Constraint
            android:id="@+id/button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent">
            <CustomAttribute
                app:attributeName="backgroundColor"
                app:customColorValue="#9999FF" />
        </Constraint>
    </ConstraintSet>

    <KeyFrameSet>
        <!-- 第一个KeyPosition代表button按钮在运行道30%的时候，相对于运行轨迹x已经运行了85%了。
        第二个KeyPosition代表button按钮在运行道60%的时候，相对于运行轨迹x已经运行了100% -->
        <KeyPosition
            app:framePosition="30"
            app:keyPositionType="pathRelative"
            app:motionTarget="@+id/button"
            app:percentY="0.4" />

        <KeyPosition
            app:framePosition="60"
            app:keyPositionType="pathRelative"
            app:motionTarget="@+id/button"
            app:percentY="-0.4" />


        <KeyPosition
            app:framePosition="80"
            app:keyPositionType="pathRelative"
            app:motionTarget="@+id/button"
            app:percentY="0.4"
            app:transitionEasing=""
            />
    </KeyFrameSet>
</MotionScene>